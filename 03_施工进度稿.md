# 《众包站长模拟器》施工进度稿 v1.0

> 交付目标:单个可部署的 `index.html`(图片base64内联)或 `游戏.zip`(html+assets)。以下按阶段推进,每阶段结束都有**可打开验收**的产物。总计约 6 个工作段(一个工作段≈一次开发会话)。

---

## 阶段总览

| 阶段 | 内容 | 产物 | 验收标准 |
|---|---|---|---|
| P0 | 文档定稿(本三稿) | 三份md | 你确认玩法/剧情/资产清单 ✅当前 |
| P1 | 核心框架 | `index.html` 灰盒版 | 用占位色块跑通30日循环:决策→结算→数值变化→游戏结束 |
| P2 | 事件系统 + 随机池 | 灰盒版v2 | 事件卡弹窗、25+随机事件可触发、数值联动正确 |
| P3 | 四条剧情线 + 结局 | 灰盒版v3 | 线A→死刑、线B/C/D、5种结局全部可达(附调试面板快进验证) |
| P4 | 美术接入 + UI打磨 | 正式版beta | GPT图替换占位;立绘切换、飘字、音效(可选)、雨天滤镜 |
| P5 | 平衡、打包、部署 | 最终 `index.html` | 完整通关30-60分钟;图片内联为base64单文件;给出部署指引 |

## 各阶段明细

### P1 · 核心框架(先跑起来)
- 游戏状态机:晨会/决策/事件/结算 四阶段切换
- 六项核心数值 + 顶栏HUD + 骑手池数据结构(姓名随机生成)
- 派单强度/罚款/克扣的决策开关及收入公式
- 日结算表 + localStorage存档 + 重开
- **依赖:无。可立即开工。**

### P2 · 事件系统
- 事件数据表结构(条件、权重、选项、效果、后续链)
- 随机池25张事件全文案
- 飘字反馈、事件插图占位框
- **依赖:P1。**

### P3 · 剧情线与结局(工作量最大)
- 线A贩毒:解锁→灰色生意开关→钓鱼→被捕→审讯→死刑CG(恶行清单回放)
- 线B母亲被撞 + 探望链;线C高考→儿子入职→车祸;线D亲哥3日倒计时→半挂事故→赔偿金讽刺结算
- 5结局判定优先级 + 结局画面(含"善终"的代价清单)
- 调试面板(改数值/跳日期,发布时隐藏)
- **依赖:P2。文案量约1.5万字,可拆两次会话。**

### P4 · 美术接入
- **你并行做**:P1-P3期间按《美术规格稿》让GPT出图,P0优先级那8张先出,放入 `assets/` 对应目录、按规范命名
- 我接图:立绘表情切换逻辑、事件插图绑定、按钮/卡框贴图、CSS雨天/夜晚滤镜
- 图没齐不阻塞:缺图自动回退占位样式
- **依赖:P3 + 至少P0优先级图。**

### P5 · 平衡与发布
- 数值平衡:确保正常游玩30日内至少踩中1-2条剧情线;死刑线利润诱惑足够"香"
- 全结局通关自测 + 修bug
- 打包:脚本把 assets 全部转 base64 内联 → 单文件 `index.html`(预计3-8MB);若超10MB则改用zip方案
- 部署指引:GitHub Pages(推荐,免费+自定义域名)或 itch.io(自带游戏页/统计),两者都是拖文件即上线
- **依赖:P4。**

## 里程碑与你需要做的事

| 时点 | 你的动作 |
|---|---|
| 现在 | 审三稿,提修改意见(尤其剧情线与结局是否符合预期) |
| P1交付后 | 试玩灰盒,确认手感节奏 |
| P1-P3期间 | 用美术稿的prompt让GPT出图,丢进项目文件夹 |
| P5交付后 | 上传部署,开玩 |

## 风险与预案

- **单文件体积**:37张图内联可能偏大 → 预案:事件插图降为1024×576、JPG质量80,或改zip交付。
- **GPT出图风格漂移**:严格拼接全局风格串;同角色多表情时把第一张图喂回去要求"same character"。
- **文案量大**:剧情文案全部在JS数据表里,后期你可直接改文本无需懂代码。
